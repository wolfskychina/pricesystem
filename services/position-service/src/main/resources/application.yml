server:
  port: 8087

spring:
  application:
    name: position-service
  profiles:
    active: sqlite
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:./data/position.db
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: position-service
      auto-offset-reset: latest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: false
    producer:
      acks: all
      enable-idempotence: true
      retries: 3
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

position:
  # 消费的客户成交事件 topic
  trade-topic: trade-event
  # 消费的对冲成交事件 topic
  hedge-fill-topic: hedge-fill-event
  # 库存监控配置
  inventory-check-interval-ms: 60000   # 库存检查间隔（毫秒），默认 60s
  hedge-inventory-cap: 100             # 对冲持仓库存上限（手），超过时告警

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
